@model LBP.Web.ViewModels.Liga.AddEditTemporadasViewModel

@{
    ViewBag.Title = Model.TemporadaId.HasValue ? "Editar Temporada" : "Agregar Temporada";

}

<div class="container">
    <div class="card card-register mx-auto mt-5">
        <div class="card-header">@ViewBag.Title</div>
        <div class="card-body">
            <form method="post" action="@Url.Action("AddEditTemporadas")">
                @Html.HiddenFor(X => X.TemporadaId)
                <div class="form-group">
                    <div class="form-row">
                        <div class="col-md-6">
                            <label for="exampleInputName">Nombre</label>
                            @Html.TextBoxFor(X => X.Descripcion, new { @class = "form-control",
                           placeholder = "Ingresa el Nombre",
                           style = "text-transform:uppercase",
                           type = "text"
                       })
                        </div>
                    </div>
                </div>
                <button type="submit" class="btn btn-primary ">
                    <i class="fa fa-save"></i>&nbsp;Guardar
                </button>
                <a class="btn btn-default" href="@Url.Action("LstTemporadas")">
                    <i class="fa fa-arrow-back"></i>&nbsp;Cancelar
                </a>
            </form>
        </div>
    </div>
</div>

